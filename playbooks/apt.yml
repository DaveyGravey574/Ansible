---
- hosts: "debian"
  become: true
  
  pre_tasks:
  - name: Update apt cache if needed.
    apt:
      update_cache: true
      cache_valid_time: 3600
  
  tasks:
    - name: Upgrade packages to the lastest version.
      apt:
        upgrade: yes
